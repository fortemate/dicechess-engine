name: "Security: Submit sbt Dependency Graph"

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "build.sbt"
      - "project/**"
      - ".github/workflows/dependency-graph.yaml"

permissions: {}

jobs:
  submit:
    name: Submit sbt dependencies
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Setup Java
        uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6 # v6.0.1
        with:
          distribution: temurin
          java-version: "25"
          cache: sbt
      - name: Setup sbt
        uses: sbt/setup-sbt@82da71df4e122282484a99a8d70096bc2369dbd8 # v1.5.9
      - name: Stop any persistent sbt server
        run: sbt shutdown
      - name: Submit resolved dependency graph
        uses: scalacenter/sbt-dependency-submission@d84eef4c09e633bcf5f113bcad7fd5e9af1baee9 # v3.2.3
        with:
          # v3.2.3 of the action still defaults to plugin v3.2.2.
          sbt-plugin-version: 3.2.3
